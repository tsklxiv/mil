# Mil provides registers for storing/loading values
# each of the registers are named after a character
# Unlike dc, registers can only store numbers
# Mil uses `s` for storing and `l` for loading registers
50 sa la .
